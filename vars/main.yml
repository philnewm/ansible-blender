---

blender_mirror_src_url: "https://download.blender.org/release/Blender{{ blender_version_short }}/blender-{{ blender_version_long }}-linux-{{ blender_architecture }}.tar.xz"

blender_dependencies:
  RedHat:
    - libX11
    - libXrender
    - libXfixes
    - libXi
    - libxkbcommon
    - libSM
    - libglvnd-glx
  Debian:
    - libx11-6
    - libxrender1
    - libxfixes3
    - libxi6
    - libxkbcommon0
    - libsm6
    - libgl1

unarchive_dependencies:
  RedHat:
    - tar
    - xz
  Debian:
    - tar
    - xz-utils
    - gzip
    - bzip2
    - zstd

...
